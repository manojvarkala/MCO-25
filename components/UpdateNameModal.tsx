import React, { FC } from 'react';

// This component is deprecated. The name update logic has been moved to the
// WordPress backend as part of the SSO login flow to ensure a valid name
// is present before the user enters the React application.

const UpdateNameModal: FC = () => {
    return null;
};

export default UpdateNameModal;
